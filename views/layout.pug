doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
  body

    h2 Oil Level &amp; Tire Pressure Reminders

    p Get a monthly SMS reminder when you stop at a gas station

    if user
      p
        a(href='/logout')
          | Logout

    if flash
      if flash.success
        div.msg(class='success')= flash['success']
      if flash.warn
        div.msg(class='warn')= flash['warn']

    block content
